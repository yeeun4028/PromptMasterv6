<Window x:Class="PromptMasterv5.NameInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重命名" Height="180" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="#FAFAFA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="请输入新的名称:" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- 已删除 SelectAllOnFocus 属性 -->
        <TextBox x:Name="InputBox" 
                 Grid.Row="1"
                 Padding="5"
                 FontSize="14"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="取消" IsCancel="True" Margin="0,0,10,0" Padding="15,5"/>
            <Button Content="确定" IsDefault="True" Click="Ok_Click" Padding="15,5" Background="#333" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>