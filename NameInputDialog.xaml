<Window x:Class="PromptMasterv6.NameInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重命名" Width="380" MinHeight="180"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height"
        Style="{DynamicResource ModernDialogWindowStyle}">

    <Grid>
        <!-- Entry Animation: Fade-in + Slide-up -->
        <Grid.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" Y="10"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:0.22">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="SlideTransform"
                                         Storyboard.TargetProperty="Y"
                                         From="10" To="0" Duration="0:0:0.22">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="重命名" Style="{StaticResource SectionHeaderTextStyle}"/>

        <!-- Helper hint text -->
        <TextBlock Grid.Row="1" Text="输入新名称，按回车确认"
                   Style="{StaticResource HintTextStyle}"
                   Margin="0,0,0,16"/>

        <TextBox x:Name="InputBox"
                 Grid.Row="2"
                 Style="{StaticResource ModernTextBoxStyle}"
                 AcceptsReturn="False"
                 VerticalAlignment="Center"
                 Margin="0,0,0,24"
                 TextChanged="InputBox_TextChanged"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" IsCancel="True" Style="{StaticResource TextLinkButtonStyle}" Margin="0,0,16,0"/>
            <Button x:Name="OkButton" Content="确定" IsDefault="True" Click="Ok_Click"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>