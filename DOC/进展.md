# 项目进展记录

## 任务：添加提示词业务逻辑分析
- **状态**：已完成
- **日期**：2026-03-07

### 已完成事项：
1. **源码调研**：深入研究了 `PromptItem` 模型、`SidebarViewModel` 创建逻辑、`MainViewModel` 导入与保存流程。
2. **数据流分析**：分析了从 UI 操作到后台数据服务（WebDAV/本地文件）的完整消息传播和节流保存机制。
3. **文档沉淀**：已在 `DOC` 目录下生成详细的分析文档 [提示词添加逻辑分析.md](file:///e:/01_代码仓库/Github/PromptMasterv6/DOC/提示词添加逻辑分析.md)。

## 任务：重构提示词添加与编辑逻辑
- **状态**：已完成
- **日期**：2026-03-07

### 已完成事项：
1. **模型增强**：在 `PromptItem` 中新增 `IsRenaming` 属性，用于控制内联编辑状态。
2. **列表区内联编辑**：
   - 点击“新建文件”后，列表自动锁定新项并进入全选编辑模式。
   - 支持回车/失去焦点自动保存，Esc 键取消编辑。
   - 增加右键“重命名”功能。
3. **内容区视觉净化**：
   - 彻底移除 Block 3 的标题编辑/显示区域，消除视觉冗余。
   - 调整控制按钮（备份、加锁、最大化、关闭）至顶部 40px 高度的拖拽把手区，实现全应用高度统一。
4. **异常修复**：解决了 XAML 样式中 `ContextMenu` 无法直接绑定代码后置事件导致的 `InvalidCastException`。

## 任务：移除主界面顶部关闭按钮 (极致极简重构)
- **状态**：已完成
- **日期**：2026-03-07

### 已完成事项：
1. **采访模式分析**：通过苏格拉底式提问确认了极简主义核心动机。
2. **UI 精简实施**：
   - 移除了 `MainWindow.xaml` 中的物理“关闭”和“最大化”按钮。
   - 剩余的业务按钮（备份、加锁）已自动平移至靠右对齐。
3. **交互规范**：确认通过 `Alt+F4` 和窗口双击管理窗口生命周期。
4. **视觉验证**：顶部 40px 的把手区现在更加整洁，仅保留核心业务入口。

## 任务：Block2 与 Block3 首行对齐优化（第一阶段）
- **状态**：已完成
- **日期**：2026-03-08

### 已完成事项：
1. **结构偏差修正**：识别并移除了 Block3 内部多余的 `RowDefinition`，统一采用悬浮重叠布局。
2. **Margin 分离诊断**：剥离了全 Block3 统一的外层 Margin，改为对 `TextBox`（编辑）和 `MarkdownViewer`（预览）分别定位。
3. **空间结构梳理**：通过 PlainText 和专业 UI 布局图明确了 Block 3 的三层 Grid 布局及内部组件层级，为后续的像素级对齐提供了直观依据。

---

*注：该进展文档根据用户要求定期更新。*

## 任务：垂直切片架构重构（Jimmy Bogard 方法论）
- **状态**：进行中
- **日期**：2026-03-09

### 已完成事项：

#### Phase 1: 提取共享服务 ✅
创建了 `Features/Shared/Services/` 目录，提取了 3 个共享服务：
- **VariableService**：变量解析、内容编译
- **ContentConverterService**：HTML ↔ Markdown 转换
- **WebTargetService**：网页目标发送逻辑

#### Phase 2: 重构 ViewModel ✅
| ViewModel | 重构前行数 | 重构后行数 | 减少 |
|-----------|-----------|-----------|------|
| MainViewModel | 894 | 717 | -177 (20%) |
| WorkspaceViewModel | 536 | 377 | -159 (30%) |

**移除的重复代码**：变量解析、HTML转换、WebTarget发送等 8 个重复方法

### 待执行：
- ✅ Phase 3: 拆分 SettingsViewModel（发现已存在，修复 DI 注册）
- ✅ Phase 4: 迁移 Messages 到各 Feature 内
- ✅ Phase 5: 清理无用的 using 语句和未使用文件
- ✅ Phase 6: 迁移对话框窗口到对应 Feature
- ✅ Phase 7: 拆分 SettingsViewModel 为子 ViewModel 并聚合
- ✅ Phase 8: 完成 XAML 绑定兼容性修复

### 当前架构状态：
```
Features/
├── Main/                    # 主窗口
│   ├── MainViewModel.cs     # 717 行（原 894 行）
│   ├── MainWindow.xaml
│   ├── LaunchBarWindow.xaml
│   └── Messages/            # ✅ 新增
├── Workspace/               # 工作区
│   └── WorkspaceViewModel.cs # 377 行（原 536 行）
├── Sidebar/                 # 侧边栏
├── Launcher/                # 启动器
│   └── Messages/            # ✅ 新增
├── ExternalTools/           # 外部工具
│   ├── Messages/            # ✅ 新增
│   └── Dialogs/             # ✅ 新增
├── Settings/                # 设置
│   ├── SettingsViewModel.cs # 聚合器（含子 VM + SettingsVM 属性）
│   ├── SettingsContainerViewModel.cs # 备用聚合器
│   ├── AiModels/            # ✅ 新增
│   │   └── AiModelsViewModel.cs
│   ├── ApiProviders/        # ✅ 新增
│   │   └── ApiProvidersViewModel.cs
│   ├── Sync/                # ✅ 新增
│   │   └── SyncViewModel.cs
│   ├── Launcher/            # ✅ 新增
│   │   └── LauncherSettingsViewModel.cs
│   ├── SettingsWindow.xaml
│   └── BackupSelectionDialog.xaml
├── PinToScreen/             # 贴图功能
│   └── PinToScreenWindow.xaml
└── Shared/                  # 共享组件
    ├── Services/            # ✅ 新增
    │   ├── VariableService.cs
    │   ├── ContentConverterService.cs
    │   └── WebTargetService.cs
    ├── Messages/            # ✅ 新增
    └── Dialogs/             # ✅ 新增
        └── ConfirmationDialog.xaml
```

### 已清理：
- ✅ 删除 `ViewModels/PromptGroup.cs`（未使用）
- ✅ 删除 `Views/sync_new.txt`（无关文件）
- ✅ 移除 `App.xaml.cs` 中的 `using PromptMasterv6.ViewModels`
- ✅ 移除 `SettingsView.xaml.cs` 中的 `using PromptMasterv6.ViewModels`
- ✅ 移除 `SettingsWindow.xaml.cs` 中的 `using PromptMasterv6.ViewModels`
- ✅ 移除 `App.xaml` 中的 `xmlns:vm` 和 `xmlns:views`
- ✅ 迁移 `PinToScreenWindow` → `Features/PinToScreen/`
- ✅ 迁移 `ConfirmationDialog` → `Features/Shared/Dialogs/`
- ✅ 迁移 `BackupSelectionDialog` → `Features/Settings/`
- ✅ 迁移 `OcrNotConfiguredDialog` → `Features/ExternalTools/Dialogs/`
- ✅ 拆分 SettingsViewModel → AiModelsVM + ApiProvidersVM + SyncVM + LauncherSettingsVM
- ✅ SettingsViewModel 聚合子 ViewModel 并注入 DI
- ✅ 添加 `SettingsVM => this` 属性保持 XAML 绑定兼容

### 详细文档：
参见 [vertical-slice-refactoring.md](file:///e:/01_代码仓库/Github/PromptMasterv6/DOC/vertical-slice-refactoring.md)
