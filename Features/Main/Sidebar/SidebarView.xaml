<UserControl x:Class="PromptMasterv6.Features.Main.Sidebar.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:PromptMasterv6.Infrastructure.Behaviors"
             xmlns:dd="urn:gong-wpf-dragdrop"
             xmlns:utils="clr-namespace:PromptMasterv6.Infrastructure.Converters"
             mc:Ignorable="d" d:DesignWidth="60" d:DesignHeight="400">

    <UserControl.Resources>
        <utils:StringToGeometryConverter x:Key="StringToGeometryConverter"/>
    </UserControl.Resources>

    <Border Background="{DynamicResource Block1Background}" CornerRadius="5,0,0,5">
        <Grid>
            <ListBox ItemsSource="{Binding Folders}" 
                     SelectedItem="{Binding SelectedFolder}"
                     ItemContainerStyle="{StaticResource FolderListBoxItemStyle}" 
                     Background="Transparent" 
                     BorderThickness="0" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                     VerticalAlignment="Top" 
                     Margin="0,15,0,0"
                     dd:DragDrop.IsDragSource="True" 
                     dd:DragDrop.IsDropTarget="True"
                     dd:DragDrop.DropHandler="{Binding FolderDropHandler}"/>

            <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,20">
                <Button Style="{StaticResource ActionBtnStyle}" Content="新建文档" Command="{Binding CreateFileCommand}">
                    <Button.ContextMenu>
                        <ContextMenu Style="{StaticResource ModernContextMenuStyle}">
                            <MenuItem Header="设置图标"
                                      Style="{StaticResource ModernMenuItemStyle}"
                                      Command="{Binding PlacementTarget.DataContext.ChangeActionIconCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="CreateFile"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource Block1ToolTipStyle}" Content="新建文档"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Path Data="{Binding DataContext.ActionIcons[CreateFile], RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource StringToGeometryConverter}, FallbackValue={StaticResource IconNewDoc}}" Style="{StaticResource ActionIconPathStyle}"/>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
                <Button Style="{StaticResource ActionBtnStyle}" Content="新建文件夹" Command="{Binding CreateFolderCommand}">
                    <Button.ContextMenu>
                        <ContextMenu Style="{StaticResource ModernContextMenuStyle}">
                            <MenuItem Header="设置图标"
                                      Style="{StaticResource ModernMenuItemStyle}"
                                      Command="{Binding PlacementTarget.DataContext.ChangeActionIconCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="CreateFolder"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource Block1ToolTipStyle}" Content="新建文件夹"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Path Data="{Binding DataContext.ActionIcons[CreateFolder], RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource StringToGeometryConverter}, FallbackValue={StaticResource IconNewFolder}}" Style="{StaticResource ActionIconPathStyle}"/>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
                <Button Style="{StaticResource ActionBtnStyle}" Content="导入" Command="{Binding ImportMarkdownFilesCommand}">
                    <Button.ContextMenu>
                        <ContextMenu Style="{StaticResource ModernContextMenuStyle}">
                            <MenuItem Header="设置图标"
                                      Style="{StaticResource ModernMenuItemStyle}"
                                      Command="{Binding PlacementTarget.DataContext.ChangeActionIconCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="Import"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource Block1ToolTipStyle}" Content="导入 Markdown 文件"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Path Data="{Binding DataContext.ActionIcons[Import], RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource StringToGeometryConverter}, FallbackValue={StaticResource IconImport}}" Style="{StaticResource ActionIconPathStyle}"/>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
                <Button Style="{StaticResource ActionBtnStyle}" Content="设置" Command="{Binding OpenSettingsCommand}">
                    <Button.ContextMenu>
                        <ContextMenu Style="{StaticResource ModernContextMenuStyle}">
                            <MenuItem Header="设置图标"
                                      Style="{StaticResource ModernMenuItemStyle}"
                                      Command="{Binding PlacementTarget.DataContext.ChangeActionIconCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="Settings"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource Block1ToolTipStyle}" Content="设置"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Path Data="{Binding DataContext.ActionIcons[Settings], RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource StringToGeometryConverter}, FallbackValue={StaticResource IconSettings}}" Style="{StaticResource ActionIconPathStyle}"/>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
                <Button Command="{Binding ToggleEditModeCommand}" Style="{StaticResource ActionBtnStyle}">
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource Block1ToolTipStyle}">
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                                <Setter Property="Text" Value="点击锁定并保存"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsEditMode}" Value="False">
                                                <Setter Property="Text" Value="点击解锁编辑"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Grid>
                                <Path Data="{StaticResource IconLockOpenThin}">
                                    <Path.Style>
                                        <Style TargetType="Path" BasedOn="{StaticResource ActionIconPathStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Path.Style>
                                </Path>
                                <Path Data="{StaticResource IconLockClosedThin}">
                                    <Path.Style>
                                        <Style TargetType="Path" BasedOn="{StaticResource ActionIconPathStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Path.Style>
                                </Path>
                            </Grid>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
                <Button Command="{Binding ManualBackupCommand}" Style="{StaticResource ActionBtnStyle}">
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource Block1ToolTipStyle}" Content="备份到云端"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Path x:Name="IconPath" 
                                  Data="{StaticResource IconCloudUpload}"
                                  Style="{StaticResource ActionIconPathStyle}"/>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding DataContext.IsDirty, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <Setter TargetName="IconPath" Property="Fill" Value="#E53935"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
