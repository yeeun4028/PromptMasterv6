<UserControl x:Class="PromptMasterv6.Features.Settings.Proxy.ProxyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="500">
    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <TextBlock Text="🌐 网络与代理设置" Style="{StaticResource SectionHeaderTextStyle}"/>
            
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="全局 VPN 代理地址" Style="{StaticResource FieldLabelTextStyle}"/>
                    <TextBlock Text="例如 v2rayN 默认值为 http://127.0.0.1:10808。此地址仅对配置为【走VPN代理】的模型生效。" 
                               Style="{StaticResource HintTextStyle}" 
                               Margin="0,2,0,0"
                               TextWrapping="Wrap"
                               MaxWidth="400"/>
                </StackPanel>
                <TextBox Grid.Column="1" 
                         Text="{Binding Config.ProxyAddress, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                         Style="{StaticResource ModernTextBoxStyle}"
                         Width="250"
                         VerticalAlignment="Center"
                         Height="30"
                         Padding="8,4"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
