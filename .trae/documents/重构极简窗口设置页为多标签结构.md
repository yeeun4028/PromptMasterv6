我将根据您的最新指示，对“极简窗口”设置页面进行多标签重构。

**具体分类计划：**

1.  **[提示词] 标签页** (Tag="0")：
    *   预选提示词列表 (ComboBox 用于添加，ListBox 用于展示和排序)

2.  **[热键] 标签页** (Tag="1")：
    *   AI查询启用开关
    *   提示词匹配前缀 (TextBox)
    *   AI回复后清空开关
    *   底部显示图标开关
    *   点击填入完整内容开关
    *   底部说明文字

3.  **[位置] 标签页** (Tag="2")：
    *   使用默认位置开关
    *   默认左下角坐标设置
    *   默认尺寸设置

**实施步骤：**

1.  **UI 结构调整 (`SettingsView.xaml`)**：
    *   在 Tab 4 顶部添加 `StackPanel` 导航栏，包含“提示词”、“热键”、“位置”三个按钮。
    *   创建三个 `StackPanel` 作为内容容器：`MiniWindowPromptTab`、`MiniWindowHotkeyTab`、`MiniWindowLocationTab`。
    *   将现有的 UI 元素按上述分类移动到对应的容器中。

2.  **后台逻辑实现 (`SettingsView.xaml.cs`)**：
    *   添加状态变量 `_selectedMiniWindowSubTab`。
    *   实现 `MiniWindowSubTab_Click` 事件处理。
    *   实现 `UpdateMiniWindowSubTab` 方法控制显示/隐藏。
    *   初始化时默认选中第一个标签。

待办事项列表已更新，准备开始执行。