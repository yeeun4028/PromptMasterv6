## 目标（本阶段只做设置页）
- 外部工具设置页面顶部改为标签页：主界面 / 百度 / 腾讯云 / 有道 / AI。
- 主界面 Tab 提供 4 个开关（百度/腾讯云/有道/AI），允许多选，并用于同时控制“翻译 + OCR”（仅保存配置，本阶段不改执行逻辑）。
- 各供应商 Tab 使用真实字段名（不再 Key1/Key2）。
- AI Tab：
  - 翻译提示词支持从 Block2 提示词库下拉选择 + 一键跳转编辑。
  - AI 模型配置支持“保存/收藏”，并可鼠标点选一条收藏来快速替换当前模型配置。

## 配置模型（AppConfig）
- 新增 `ExternalToolsSettings`（或等价字段集合），包含：
  - `EnableBaidu/EnableTencent/EnableYoudao/EnableAi`
  - 百度：`BaiduTranslateAppId/BaiduTranslateSecret/BaiduOcrApiKey/BaiduOcrSecret`
  - 腾讯云：`TencentSecretId/TencentSecretKey/TencentRegion`
  - 有道：`YoudaoAppKey/YoudaoAppSecret`
  - AI（独立一套，不复用聊天 AI）：
    - 当前生效：`AiTranslateBaseUrl/AiTranslateApiKey/AiTranslateModel`
    - 提示词：`AiTranslatePromptFileId` + `AiTranslateDefaultPrompt`（默认：你是一个专业的翻译助手。请将接收到的内容翻译成中文）
    - 模型收藏：`AiTranslateSavedModels : ObservableCollection<AiModelConfig>`（复用现有 [AiModelConfig](file:///e:/aDrive_backup/Projects/PromptMasterv5/PromptMasterv5/Core/Models/AiModelConfig.cs) 结构）
    - （可选）`AiTranslateActiveModelId`：记录当前选中的收藏项，用于 UI 高亮/记忆
- 兼容迁移：若检测到旧 `ApiProfiles/TranslateProfileId/OcrProfileId` 但新字段为空，则尽量映射填充到新字段（不删除旧数据）。

## 设置页 UI（SettingsView.xaml）
### 1) Tab 结构
- 外部工具区域改成 TabControl：主界面/百度/腾讯云/有道/AI。

### 2) 主界面 Tab
- 4 个 ToggleSwitch/CheckBox：百度/腾讯云/有道/AI（绑定 Enable*）。
- 提示文本：可多选；后续翻译/OCR 将从已启用供应商中择优（最快者胜）。

### 3) 百度/腾讯云/有道 Tab
- 按真实字段名展示输入框并绑定配置字段。

### 4) AI Tab
- 基础配置：BaseUrl/ApiKey/Model 输入框（绑定当前生效字段）。
- 提示词选择：
  - 下拉框 ItemsSource=Block2 提示词列表（Files），SelectedValue 绑定 `AiTranslatePromptFileId`。
  - “在 Block2 打开/编辑”按钮：选中该提示词并进入编辑模式。
- 模型收藏区（满足你说的“鼠标点选替换模型”）：
  - 列表展示 `AiTranslateSavedModels`（显示 ModelName 或自定义名称）。
  - 点击某一项：把该项的 BaseUrl/ApiKey/ModelName 写回当前生效字段（立即替换）。
  - “保存当前为收藏”按钮：把当前 BaseUrl/ApiKey/Model 追加到收藏列表。
  - “删除选中收藏”按钮：从收藏列表移除。

## 交互跳转（仅用于设置页操作）
- MainViewModel 新增命令：根据 `AiTranslatePromptFileId` 定位到对应 PromptItem，触发 Block2 选中并进入编辑模式（复用现有选中文件/编辑模式机制）。

## 验证
- 设置页标签与布局正确。
- 4 个开关、各供应商凭证、AI 模型收藏都能保存/重启恢复。
- AI 下拉可选 Block2 提示词，点击“打开/编辑”可跳转并进入编辑。
- 构建通过。

你确认后我将按以上方案开始输出代码修改；本阶段只交付“设置页 + 配置持久化/迁移 + AI 提示词选择 + AI 模型收藏与点选替换”，不在本阶段接入腾讯云/有道/AI 的真实翻译 API 调用逻辑。