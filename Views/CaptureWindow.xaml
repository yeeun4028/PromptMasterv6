<Window x:Class="PromptMasterv5.Views.CaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PromptMasterv5.ViewModels"
        xmlns:md="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
        mc:Ignorable="d"
        Title="QuickAction" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="600"
        SizeToContent="Height"
        WindowStartupLocation="Manual">

    <Window.DataContext>
        <vm:CaptureViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Border Background="#1E1E1E" CornerRadius="8" BorderBrush="#333333" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.4"/>
        </Border.Effect>
        
        <StackPanel>
            <!-- 顶部搜索/标识栏 -->
            <Grid Height="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="⚡ PromptMaster" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="15,0"/>
                
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Background="Transparent" BorderThickness="0" Foreground="White"
                         VerticalAlignment="Center" FontSize="16"
                         CaretBrush="White"/>
            </Grid>

            <!-- 指令列表 -->
            <ItemsControl ItemsSource="{Binding QuickActions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.ExecuteActionCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                CommandParameter="{Binding}"
                                Background="Transparent" BorderThickness="0"
                                HorizontalContentAlignment="Left" Padding="15,8">
                            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="14"/>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- 结果区域 (动态展开) -->
            <StackPanel Visibility="{Binding ResultText, Converter={StaticResource BooleanToVisibilityConverter}}"> <!-- 需要 Converter -->
                <Border Height="1" Background="#333333" Margin="0,5"/>
                
                <md:MarkdownViewer Markdown="{Binding ResultText}" 
                                   Margin="15" 
                                   Foreground="#DDDDDD">
                    <md:MarkdownViewer.Resources>
                        <Style TargetType="FlowDocument">
                            <Setter Property="FontFamily" Value="Segoe UI"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="TextAlignment" Value="Left"/>
                            <Setter Property="PagePadding" Value="0"/>
                        </Style>
                        <Style TargetType="Paragraph">
                            <Setter Property="Margin" Value="0,0,0,10"/>
                        </Style>
                    </md:MarkdownViewer.Resources>
                </md:MarkdownViewer>
                
                <!-- 底部操作栏 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                    <Button Content="复制" Command="{Binding CopyCommand}" Margin="5,0"/>
                    <Button Content="替换" Command="{Binding ReplaceCommand}" Margin="5,0"/>
                </StackPanel>
                
                <!-- 追问输入框 -->
                <Grid Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Text="{Binding FollowUpText, UpdateSourceTrigger=PropertyChanged}" 
                             Foreground="White" 
                             Background="#333333" 
                             BorderThickness="0" 
                             Padding="8"
                             FontSize="13">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SendFollowUpCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    
                    <Button Grid.Column="1" Content="发送" Command="{Binding SendFollowUpCommand}" Margin="5,0,0,0" Padding="10,5"/>
                </Grid>
            </StackPanel>
            
            <!-- 加载动画 -->
            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,10">
                <ProgressBar IsIndeterminate="True" 
                             Height="4" 
                             Background="Transparent" 
                             BorderThickness="0" 
                             Foreground="#007ACC"
                             HorizontalAlignment="Stretch"/>
                <TextBlock Text="AI 正在思考中..." 
                           Foreground="#888888" 
                           FontSize="12" 
                           HorizontalAlignment="Center" 
                           Margin="0,8,0,0"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>