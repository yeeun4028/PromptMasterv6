<Window x:Class="PromptMasterv6.Views.TranslationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="翻译结果" 
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight">

    <Border Background="#363B40" 
            CornerRadius="8"
            Padding="15">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="3" Opacity="0.3"/>
        </Border.Effect>
        
        <Grid>
            <!-- Translation Result -->
            <TextBox x:Name="ResultBox"
                     Text="Loading..."
                     TextWrapping="Wrap"
                     BorderThickness="0"
                     Background="Transparent"
                     Foreground="#b8bfc6"
                     FontFamily="Segoe UI, Microsoft YaHei UI, Microsoft YaHei, Sans-Serif"
                     FontSize="16"
                     IsReadOnly="True"
                     MaxWidth="500"
                     TextAlignment="Left"
                     Margin="0,0,0,0">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Paragraph}">
                        <Setter Property="LineHeight" Value="26"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
        </Grid>
    </Border>
</Window>