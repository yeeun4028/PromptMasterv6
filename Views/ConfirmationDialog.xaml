<Window x:Class="PromptMasterv5.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认" Height="220" Width="420"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource ModernDialogWindowStyle}">

    <Grid>
        <!-- Entry Animation -->
        <Grid.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" Y="10"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
                        <DoubleAnimation Storyboard.TargetName="SlideTransform" Storyboard.TargetProperty="Y" From="10" To="0" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock x:Name="TitleText" Text="未保存的更改" Style="{StaticResource SectionHeaderTextStyle}"/>

        <!-- Message Content -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <Viewbox Width="24" Height="24" Margin="0,0,12,0" VerticalAlignment="Top">
                     <Path Data="M13,13H11V7H13M13,17H11V15H13M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z" 
                           Fill="#FFC107" Stretch="Uniform"/>
                </Viewbox>
                <TextBlock Text="您有未备份的修改。是否在退出前备份到云端？"
                           Style="{StaticResource FieldLabelTextStyle}"
                           Foreground="{DynamicResource TextPrimary}"
                           FontSize="15"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Width="300"/>
            </StackPanel>
            <TextBlock Text="提示：选择【直接退出】将仅保存到本地，不会上传至 WebDAV。"
                       Style="{StaticResource HintTextStyle}"
                       Margin="36,0,0,0" Opacity="0.8"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="取消" IsCancel="True" Click="Cancel_Click" Style="{StaticResource TextLinkButtonStyle}" Margin="0,0,16,0"/>
            <Button Content="直接退出" Click="No_Click" Style="{StaticResource DangerLinkButtonStyle}" Margin="0,0,16,0"/>
            <Button Content="备份并退出" IsDefault="True" Click="Yes_Click" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
