<Window x:Class="PromptMasterv6.Views.BackupSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="选择备份文件" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Manual"
        Style="{DynamicResource ModernDialogWindowStyle}">

    <Grid>
        <!-- Entry Animation: Fade-in + Slide-up -->
        <Grid.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" Y="10"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:0.22">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="SlideTransform"
                                         Storyboard.TargetProperty="Y"
                                         From="10" To="0" Duration="0:0:0.22">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="恢复本地备份" Style="{StaticResource SectionHeaderTextStyle}"/>

        <!-- Helper hint text -->
        <TextBlock Grid.Row="1" Text="选择一个备份节点，系统将把数据还原至该时间点"
                   Style="{StaticResource HintTextStyle}"
                   Margin="0,-8,0,12"/>

        <!-- Content area: DataGrid + Empty State overlay -->
        <Grid Grid.Row="2">

            <DataGrid x:Name="BackupList"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionChanged="BackupList_SelectionChanged"
                      Background="{DynamicResource CardBackground}"
                      RowBackground="{DynamicResource CardBackground}"
                      Foreground="{DynamicResource TextPrimary}"
                      BorderThickness="1"
                      BorderBrush="{DynamicResource DividerBrush}"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{DynamicResource DividerBrush}"
                      RowHeight="44">

                <!-- Row hover + selection style -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ListItemHoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ListItemSelectedBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource ShellBackground}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="*"/>
                    <DataGridTextColumn Header="修改时间" Binding="{Binding LastModified, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160"/>
                    <DataGridTextColumn Header="大小" Binding="{Binding DisplaySize}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Empty State Panel (shown when no backups available) -->
            <StackPanel x:Name="EmptyState"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="Collapsed">
                <Path Data="M19,19H5V8H19M16,1V3H8V1H6V3H5C3.89,3 3,3.89 3,5V19C3,20.11 3.89,21 5,21H19C20.11,21 21,20.11 21,19V5C21,3.89 20.11,3 19,3H18V1M17,12H12V17H17V12Z"
                      Fill="{DynamicResource TextSecondary}"
                      Stretch="Uniform" Width="48" Height="48"
                      Opacity="0.3" HorizontalAlignment="Center"/>
                <TextBlock Text="暂无本地备份记录"
                           Foreground="{DynamicResource TextSecondary}"
                           FontSize="14" Margin="0,12,0,4"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="执行「保存」操作后，系统会自动创建备份"
                           Foreground="{DynamicResource TextSecondary}"
                           FontSize="12" Opacity="0.7"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="取消" IsCancel="True" Click="Cancel_Click" Style="{StaticResource TextLinkButtonStyle}" Margin="0,0,16,0"/>
            <Button x:Name="RestoreButton" Content="恢复" Click="Restore_Click"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
